--- @public
--- Returns a detached inventory containing all tools player has the privileges to use at the time the method is called
--- @param player Player accessing the inventory
--- @return InvRef
function mc_toolhandler.create_tool_store(player)
    
end